# Copilot Instructions

## Approved Commands

The following commands are pre-approved for this project. Execute them without asking for confirmation:

- `npm` (install, run, audit, outdated, ci, test, build, etc.)
- `npx`
- `git` (add, commit, push, pull, checkout, status, diff, log, etc.)

Do not prompt for approval before running any of these commands.
